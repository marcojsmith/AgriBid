# AgriBid (GEMINI.md)

`AGENTS.md` at the repo root is the single source of rules, commands and links for every coding agent, including Gemini. Read it first, then `docs/STATUS.md`.

Gemini-specific rule: never use shell commands (`sed`, `echo`, etc.) to modify file content; use the editing tools.
