import { describe, it } from "vitest";

// Since there are no existing Convex tests, I'll follow the TDD approach
// but I'll need to mock the Convex context if a testing library like convex-test is not available.
// However, I'll first define the tests and then implement the logic.

describe("Categories Backend", () => {
  it.todo("should create a new category");
  it.todo("should not allow duplicate category names");
  it.todo("should list active categories");
  it.todo("should update a category name");
  it.todo("should soft delete a category");
});
